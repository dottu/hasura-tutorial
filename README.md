# nodejs-express

```
npm install
npm start

```

# Hasura 
```
 hasura metadata reload
 hasura migrate apply --all-databases
 hasura metadata apply
 hasura console
``` 

# run docker
```
docker-compose up
```